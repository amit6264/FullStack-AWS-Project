pipeline {
    agent any

    environment {
        AWS_ACCOUNT_ID = "066380524960"
        AWS_REGION     = "eu-north-1"
        ECR_REPO       = "frontend"
        IMAGE_NAME     = "frontend"
        GIT_REPO_NAME  = "FullStack-AWS-Project"
        GIT_USER_NAME  = "amit6264"
    }

    stages {

        stage('Cleaning Workspace') {
            steps {
                cleanWs()
            }
        }

        stage('Checkout from Git') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/amit6264/FullStack-AWS-Project.git'
            }
        }

        stage("Docker Image Build") {
            steps {
                dir('client') {
                    sh '''
                        docker system prune -f
                        docker container prune -f
                        docker build -t ${IMAGE_NAME} .
                    '''
                }
            }
        }

        stage("ECR Image Pushing") {
            steps {
                sh '''
                    aws ecr get-login-password --region ${AWS_REGION} \
                    | docker login --username AWS --password-stdin \
                    ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com

                    docker tag ${IMAGE_NAME}:latest \
                    ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPO}:${BUILD_NUMBER}

                    docker push \
                    ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPO}:${BUILD_NUMBER}
                '''
            }
        }

        stage('Update Deployment file') {
            steps {
                dir('kubernetes-files') {
                    withCredentials([string(credentialsId: 'my-git-pattoken', variable: 'git_token')]) {
                        sh '''
                            git config user.email "amit.patidar6264@gmail.com"
                            git config user.name "amit6264"

                            sed -i "s#image:.*#image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPO}:${BUILD_NUMBER}#g" frontend-deploy-service.yaml

                            git add .
                            git commit -m "Update frontend image to version ${BUILD_NUMBER}"
                            git push https://${git_token}@github.com/${GIT_USER_NAME}/${GIT_REPO_NAME}.git HEAD:main
                        '''
                    }
                }
            }
        }
    }
}
